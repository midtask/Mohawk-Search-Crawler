How to use: 

First, specify which URL to crawl from in seeds.txt, one URL per line.
Run the spider.pl script from command line. It will start crawling. 
For example, >perl spider.pl 

The crawler will make a text file separated by tab containing URL, keywords, description called spider.dat once it finishes crawling or is terminated by user.